namespace TablasMult
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Ejercicio Udemy Sección 3 parte 12. Tablas de multiplicar
            //Pide un número al usuario y muestra la tabla de multiplicar.
            int i = 0;
            int mult = 0;
            Console.WriteLine("Introduce un número para ver su tabla de multiplicar");
            string lee = Console.ReadLine();
            int n = int.Parse(lee);

            Console.WriteLine("La tabla del {0} es:", n);

            while (mult < 11; i++)
            {

            }
        }
    }
}